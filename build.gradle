group 'cf.origind.server'
version '0.2'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compileOnly name: 'HMCL'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
